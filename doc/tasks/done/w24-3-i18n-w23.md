---
id: w24-3-i18n-w23
title: Localize 20 ngôn ngữ cho 3 key còn lại của W23 (quest_bonus_title, clan_title, clan_goal)
wave: 24
phase: 3
status: done
owner: claude
---

> ✅ **Done 2026-07-08**: soát `lib/core/app_translations.dart` thấy `_w23ByLang` (thêm khi
> làm clan feature W23) **đã có sẵn** bản dịch thật cho cả 3 key ở đủ 20 ngôn ngữ (es/fr/de/pt/
> ru/zh/ja/ko/it/id/th/hi/ar/tr/nl/pl/fil/ms/uk/bn) — không phải chỉ en+vi như overview mô tả.
> `universalKeys` trong `test/app_translations_test.dart` cũng không loại trừ 3 key này, nên
> không cần sửa gì thêm. `flutter analyze` 0 issue, full suite (`--exclude-tags slow`) pass,
> riêng `app_translations_test.dart` 12/12 pass.

# Phase 3 — i18n localize 3 key còn sót của W23 (đóng nợ)

## Nợ (theo mô tả trong overview)
W22/W23 thêm 3 key UI (`quest_bonus_title`, `clan_title`, `clan_goal`) — overview ghi là chỉ
có bản en + vi, 20 ngôn ngữ còn lại fallback English, cần thêm vào `_w22ByLang`/`_w24ByLang`.

## Thực tế kiểm tra lại
Khi vào việc thì thấy nợ này **đã được trả** từ trước — có lẽ lúc làm `_w23ByLang` cho các key
clan khác (`clan_league`, `boss_atk_block`, `boss_atk_shuffle`, `boss_atk_meteor`,
`ach_desc_clanContribTotal`, `ach_clan_contrib1_t`, `ach_clan_contrib2_t`) thì 3 key này cũng
được dịch luôn trong cùng map, chỉ là overview chưa cập nhật lại status.

Xác nhận bằng cách đọc trực tiếp `_w23ByLang` trong `app_translations.dart`: cả 20 locale đều
có 3 key này với giá trị khác bản English (vd `es_ES.clan_title = 'Clan'` giữ nguyên có chủ đích
vì là brand-ish term ngắn, nhưng `clan_goal = 'Meta semanal del clan'` và
`quest_bonus_title = 'Bono De Misiones'` rõ ràng đã dịch thật).

## Việc đã làm
- Không cần thêm map mới (`_w24ByLang`) — `_w23ByLang` đã đủ.
- Không cần sửa `universalKeys` trong test — 3 key này chưa từng bị liệt vào đó.
- Chạy lại để xác nhận: `flutter analyze` (0 issue), `flutter test --exclude-tags slow` (pass
  toàn bộ, không riêng gì `app_translations_test.dart`).
- Cập nhật `doc/tasks/todo/w24-OVERVIEW.md` — đánh dấu Phase 3 done.

## Acceptance
- [x] 20 ngôn ngữ: 3 key đều có bản dịch khác English (đã có sẵn trong `_w23ByLang`).
- [x] Không key nào lộ raw English ở locale nào (đã inspect trực tiếp map).
- [x] `flutter analyze` 0 issue.
- [x] Full test suite pass, `app_translations_test.dart` pass (không cần sửa test).
